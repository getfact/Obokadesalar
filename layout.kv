#
<HouseLayout>:
<HouseShell>:
<HouseButton>:
<Home>:
    MyActionBar:

    HouseShell:
        ScrollView:
            size: self.size
            
            HouseLayout:

                HouseButton:
                    text: 'Novahuset'
                    on_release:
                        root.set_result("Novahuset")
                        root.manager.current = 'search'
                        root.manager.transition.direction = 'left'
                        
                HouseButton:
                    text: 'Långhuset'
                    on_release:
                        root.set_result("Långhuset")
                        root.manager.current = 'search'
                        root.manager.transition.direction = 'left'
                        
                HouseButton:
                    text: 'Teknikhuset'
                    on_release:
                        root.set_result("Teknikhuset")
                        root.manager.current = 'search'
                        root.manager.transition.direction = 'left' 
                HouseButton:
                    text: 'Forumhuset'
                    on_release:
                        root.set_result("Forumhuset")
                        root.manager.current = 'search'
                        root.manager.transition.direction = 'left'                
                HouseButton:
                    text: 'Prismahuset P137-P219'
                    on_release:
                        root.set_result("Prismahuset")
                        root.manager.current = 'search'
                        root.manager.transition.direction = 'left'                
                HouseButton:
                    text: 'Prismahuset P229-P258'
                    on_release:
                        root.set_result("Prismahuset2")
                        root.manager.current = 'search'
                        root.manager.transition.direction = 'left'             
                HouseButton:
                    text: 'Prismahuset Grupprum'
                    on_release:
                        root.set_result("PrismahusetG")
                        root.manager.current = 'search'
                        root.manager.transition.direction = 'left'
                        
                HouseButton:
                    text: 'Gymnastikhuset'
                    on_release:
                        root.set_result("Gymnastikhuset")
                        root.manager.current = 'search'
                        root.manager.transition.direction = 'left'
                        
                HouseButton:
                    text: 'Bilbergskahuset'
                    on_release:
                        root.set_result("Bilbergskahuset")
                        root.manager.current = 'search'
                        root.manager.transition.direction = 'left'
                        
                HouseButton:
                    text: 'Campus USÖ'
                    on_release:
                        root.set_result("CampusUSO")
                        root.manager.current = 'search'
                        root.manager.transition.direction = 'left'
                        
                HouseButton:
                    text: 'Musikskolan'
                    on_release:
                        root.set_result("Musikskolan")
                        root.manager.current = 'search'
                        root.manager.transition.direction = 'left'
                        
                HouseButton:
                    text: 'Biblioteket Grupprum'
                    on_release:
                        root.set_result("Biblioteket")
                        root.manager.current = 'search'
                        root.manager.transition.direction = 'left'
<Search>:

    MyActionBar:
        ActionView:
            ActionPrevious:
                width: 1000

                on_release:
                    root.manager.current = 'home'
                    root.manager.transition.direction = 'right'
    Label:
        text: root.result_text
<About>: